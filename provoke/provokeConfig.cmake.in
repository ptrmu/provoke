# - Config file for the provoke package
# It defines the following variables
#  provoke_INCLUDE_DIRS - include directories for FooBar
#  provoke_LIBRARIES    - libraries to link against
#  provoke_EXECUTABLE   - the bar executable

set(provoke_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${provoke_DIR}/export_provoke.cmake")

# These are IMPORTED targets created by provokeTargets.cmake

set(provoke_EXECUTABLE provoke_node)

